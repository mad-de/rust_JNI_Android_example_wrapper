[package]
name = "rust"
version = "0.1.0"
edition = "2018"
authors = ["mad-de <cev_madde@msn.com>"]

[dependencies]
jni = { version = "0.10.2", default-features = false }

[profile.release]
lto = true

[lib]
name = "rust"
crate-type = ["cdylib"]
